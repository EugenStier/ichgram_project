import mongoose from "mongoose";
const likeSchema = new mongoose.Schema({
    post_Id: {
        type mongoose.Schema({
            post_id: {
                type: mongoose. 
            }
        })
    }
})